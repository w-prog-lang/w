// veclib imports mathlib itself; importing both must merge mathlib once
// (diamond), not twice (redefinition)
#import <libs/veclib.wlang>
#import <libs/mathlib.wlang>

fn main: int32 <- () {
    return len_sq(3, 4) + square(3);
}
